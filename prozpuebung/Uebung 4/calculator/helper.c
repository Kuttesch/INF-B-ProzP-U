#include "helper.h"
#include <stdio.h>
#include <math.h>
double sum(double a, double b ) {
    return a+b;
}

double powerOf(double a, int power) {
    return pow( a, power);
}

double dif(double a, double b) {
    return a-b;
}